# Dockerized Flask Starter App
Dockerized Flask Application with Nginx Integration

## Getting Started
### Prerequisites
* Docker
* Docker Compose

### Excuting App (in development)
```bash
# Build images
docker-compose build --no-cache

# Run images (debugging mode)
docker-compose up

# Stop sevices
docker-compose down
```
